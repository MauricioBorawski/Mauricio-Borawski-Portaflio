---
interface Props {
  aligment?: "vertical" | "horizontal";
  width?: string;
}

const { aligment, width } = Astro.props;
---

<div
  class:list={[
    "bg-gray-300 rounded",
    aligment === "horizontal" ? "w-full h-1" : "w-1 h-full",
    width ? `w-[${width}]` : "",
  ]}
>
</div>
